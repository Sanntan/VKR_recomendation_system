# Логирование и обработка ошибок - Объяснение

## 📝 Что такое логирование?

**Логирование** - это процесс записи информации о работе приложения в файлы или консоль для последующего анализа.

### Зачем нужно логирование?

1. **Отладка** - найти, где и почему произошла ошибка
2. **Мониторинг** - отслеживать работу приложения в реальном времени
3. **Анализ** - понять, как пользователи используют приложение
4. **Аудит** - запись важных действий (кто, что, когда сделал)

### Уровни логирования

```
DEBUG    → Детальная информация для разработчиков
INFO     → Обычные события (запуск, завершение операций)
WARNING  → Предупреждения (что-то не так, но работает)
ERROR    → Ошибки (что-то сломалось, но приложение продолжает работать)
CRITICAL → Критические ошибки (приложение может упасть)
```

### Примеры логов в нашем проекте

```python
# Информационное сообщение
logger.info("Пользователь авторизован", extra={"user_id": 123})

# Предупреждение
logger.warning("Медленный запрос к БД", extra={"duration": 2.5, "query": "SELECT ..."})

# Ошибка
logger.error("Не удалось загрузить данные", extra={"student_id": "abc123"})
```

**Вывод в консоль:**
```
2025-01-15 10:30:45 - vkr.api - INFO - Пользователь авторизован
2025-01-15 10:30:46 - vkr.api - WARNING - Медленный запрос к БД
2025-01-15 10:30:47 - vkr.api - ERROR - Не удалось загрузить данные
```

**Вывод в JSON (для файлов):**
```json
{
  "timestamp": "2025-01-15T10:30:45.123Z",
  "level": "INFO",
  "logger": "vkr.api",
  "message": "Пользователь авторизован",
  "module": "routes",
  "function": "login",
  "line": 42,
  "user_id": 123
}
```

---

## ⚠️ Что такое обработка ошибок?

**Обработка ошибок** - это механизм, который:
1. **Перехватывает** ошибки, когда они происходят
2. **Логирует** их для анализа
3. **Показывает** понятное сообщение пользователю
4. **Предотвращает** падение приложения

### Без обработки ошибок

```python
# Плохо: приложение упадет
def get_student(student_id):
    student = db.query(Student).filter_by(id=student_id).first()
    return student.name  # ❌ Упадет, если student = None
```

**Результат:**
```
AttributeError: 'NoneType' object has no attribute 'name'
Приложение упало, пользователь видит страшную ошибку
```

### С обработкой ошибок

```python
# Хорошо: ошибка обработана
def get_student(student_id):
    try:
        student = db.query(Student).filter_by(id=student_id).first()
        if not student:
            raise NotFoundError("Студент не найден", details={"student_id": student_id})
        return student.name
    except NotFoundError:
        # Логируем и возвращаем понятное сообщение
        logger.warning("Студент не найден", extra={"student_id": student_id})
        raise  # Передаем дальше в обработчик
```

**Результат:**
- Пользователь видит: `"Студент не найден"`
- В логах: `WARNING - Студент не найден (student_id: abc123)`
- Приложение продолжает работать

---

## 🔄 Как это работает в нашем проекте

### 1. В FastAPI (Backend)

```python
# В роутере
@router.get("/students/{student_id}")
def get_student(student_id: UUID, db: Session = Depends(db_dependency)):
    student = db.query(Student).filter_by(id=student_id).first()
    
    if not student:
        # Используем наше исключение
        raise NotFoundError(
            "Студент не найден",
            details={"student_id": str(student_id)}
        )
    
    return student
```

**Что происходит:**
1. Если студент не найден → выбрасывается `NotFoundError`
2. Обработчик ошибок (`error_handler.py`) перехватывает его
3. Логирует ошибку с контекстом
4. Возвращает JSON с понятным сообщением:
   ```json
   {
     "error": true,
     "message": "Студент не найден",
     "details": {"student_id": "..."},
     "type": "NotFoundError"
   }
   ```

### 2. В Telegram Bot

```python
async def show_recommendations(update: Update, context: ContextTypes.DEFAULT_TYPE):
    try:
        recommendations = await api_client.get_recommendations(student_id)
        # Показываем рекомендации
    except APIClientError as e:
        # Логируем ошибку
        logger.error("Ошибка загрузки рекомендаций", extra={"error": str(e)})
        # Показываем пользователю понятное сообщение
        await update.message.reply_text("❌ Не удалось загрузить рекомендации")
```

**Что происходит:**
1. Ошибка API перехватывается
2. Логируется с деталями
3. Пользователь видит понятное сообщение
4. Ошибка также отправляется в Sentry (если настроен)

### 3. В Админ-панели (Vue.js)

```javascript
// В компоненте
async function loadStudent() {
  try {
    const student = await fetchStudentByParticipant(participantId.value);
    // Показываем студента
  } catch (error) {
    // Автоматически обрабатывается через errorHandler.js
    handleError(error, {
      component: 'StudentsView',
      action: 'loadStudent'
    });
  }
}
```

**Что происходит:**
1. Ошибка API перехватывается interceptor'ом в `api/client.js`
2. Преобразуется в `AppError` с понятным сообщением
3. Показывается в компоненте `ErrorNotification.vue`
4. Логируется в консоль браузера
5. Отправляется в Sentry (если настроен)

---

## 🎯 Централизованная обработка ошибок

### Преимущества

1. **Единый формат** - все ошибки обрабатываются одинаково
2. **Автоматическое логирование** - не нужно писать `logger.error()` везде
3. **Понятные сообщения** - пользователь видит дружелюбные сообщения
4. **Отслеживание** - все ошибки попадают в Sentry

### Как это работает

```
Ошибка происходит
    ↓
Перехватывается обработчиком
    ↓
Логируется (консоль + файл)
    ↓
Отправляется в Sentry (если настроен)
    ↓
Возвращается понятное сообщение пользователю
```

---

## 📊 Sentry - Отслеживание ошибок

**Sentry** - это сервис для мониторинга ошибок в реальном времени.

### Что делает Sentry?

1. **Собирает** все ошибки из приложения
2. **Группирует** похожие ошибки
3. **Показывает** где и когда произошла ошибка
4. **Отправляет** уведомления (email, Slack и т.д.)

### Пример ошибки в Sentry

```
Ошибка: NotFoundError - Студент не найден
Частота: 15 раз за последний час
Где: GET /students/abc123
Контекст:
  - student_id: "abc123"
  - user_id: 456
  - timestamp: 2025-01-15 10:30:45
```

---

## 🔍 Практические примеры

### Пример 1: Ошибка валидации

```python
# Пользователь отправил неверные данные
@router.post("/students")
def create_student(data: StudentCreateSchema):
    # Pydantic автоматически валидирует
    # Если данные неверные → RequestValidationError
    pass
```

**Обработка:**
- Логируется как `WARNING` (не критично)
- Возвращается список ошибок валидации
- Пользователь видит, что именно не так

### Пример 2: Ошибка базы данных

```python
# Проблема с подключением к БД
student = db.query(Student).first()  # БД недоступна
```

**Обработка:**
- Логируется как `ERROR` (критично)
- Отправляется в Sentry
- Возвращается: "Ошибка работы с базой данных"
- Приложение не падает

### Пример 3: Необработанная ошибка

```python
# Неожиданная ошибка
result = 1 / 0  # ZeroDivisionError
```

**Обработка:**
- Логируется как `CRITICAL` (очень критично)
- Отправляется в Sentry с полным стеком
- Возвращается: "Внутренняя ошибка сервера"
- Разработчики получают уведомление

---

## 📈 Мониторинг и анализ

### Что можно отслеживать?

1. **Частота ошибок** - сколько раз произошла ошибка
2. **Типы ошибок** - какие ошибки самые частые
3. **Время отклика** - медленные запросы
4. **Пользователи** - кто столкнулся с ошибкой

### Примеры метрик

```
За последний час:
- 5 ошибок "Студент не найден"
- 2 ошибки "Ошибка базы данных"
- 1 ошибка "Валидация данных"
- Среднее время отклика: 150ms
```

---

## ✅ Итог

**Логирование** = запись информации о работе приложения
**Обработка ошибок** = перехват и корректная обработка ошибок

**Результат:**
- ✅ Пользователи видят понятные сообщения
- ✅ Разработчики видят детальную информацию
- ✅ Ошибки отслеживаются и анализируются
- ✅ Приложение стабильно работает

